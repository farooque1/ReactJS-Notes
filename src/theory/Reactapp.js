import React from 'react'

export default function Reactapp() {
    
    return (
        <div>                    
           <p> React is a front-end JavaScript library developed by Facebook in 2011.</p>
           <p>It follows the component based approach which helps in building reusable UI components.</p>
           <p>It is used for developing complex and interactive web and mobile UI.</p>
           <p>Even though it was open-sourced only in 2015, it has one of the largest communities supporting it.</p>
           <p>It uses the virtual DOM instead of the real DOM.</p>
           <p>It uses server-side rendering.</p>
           <p>It follows uni-directional data flow or data binding.</p>
        </div>
    )
}
